import { Home, Compass, Map, Briefcase, Bookmark, User, type LucideIcon } from "lucide-react";

export const NAV_ICONS: Record<string, LucideIcon> = {
  home: Home,
  compass: Compass,
  map: Map,
  briefcase: Briefcase,
  bookmark: Bookmark,
  user: User,
};
